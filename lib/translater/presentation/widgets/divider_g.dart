import 'package:flutter/material.dart';

class DividerG extends StatelessWidget {
  const DividerG({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      width: double.infinity,
      color: Colors.grey.shade300,
    );
  }
}
